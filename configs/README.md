# configs

Example configuration only. Do not commit production secrets.

- `nakama.yml` configures the local Nakama development server.
- `socket.server_key`, console password, and database credentials are intentionally local defaults.

